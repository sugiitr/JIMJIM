# /edit — 部分修正する

特定のコードブロック・関数・行のみをターゲットに修正する。全置換せず安全・高速。

## 手順

1. 対象箇所を特定する（ファイル・行番号・関数名）
2. 修正内容を確認する
3. `Edit` ツールで最小限の変更を適用する
4. 他のコードへの影響がないことを確認する

## ルール

- 変更は最小限にとどめる
- 関係ないコードには触れない
- 修正後は動作確認する

$ARGUMENTS に「ファイル名 修正内容」を渡してください。
例: `src/auth.ts login関数のエラーハンドリングを追加`
