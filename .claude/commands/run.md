# /run — コードを実行する

指定したコード（Python・Bash・TypeScript等）をClaudeが自動生成してそのまま実行し、結果を確認する。

## 手順

1. 実行したい処理の目的を確認する
2. 適切な言語・コマンドでスクリプトを生成する
3. `Bash` ツールで実行する
4. 結果を分析して報告する

## ルール

- 危険なコマンド（rm -rf等）は実行しない
- 本番環境への変更は確認を取る
- 実行結果を必ず報告する

$ARGUMENTS に実行したい処理を渡してください。
